# Studio Website

Standalone deployment of the Studio interactive website clone.

## Scripts

- `pnpm dev` starts the local Next.js dev server.
- `pnpm build` builds the production app.
- `pnpm typecheck` runs TypeScript without emitting files.
